@page "/league/{LeagueKey}/season/{SeasonKey}/settings"
@layout AppLayout
@using GolfManager.Web.Components.Icons

<PageTitle>Season Settings - Pinzo</PageTitle>

@if (isLoading)
{
    <div class="flex items-center justify-center py-16">
        <div class="text-center">
            <div class="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-primary-600 mb-4"></div>
            <p class="text-gray-600">Loading season settings...</p>
        </div>
    </div>
}
else if (!isAdmin)
{
    <div class="text-center py-16">
        <MaterialIcon Path="@MaterialIcons.LockClosed" Size="MaterialComponents.Icons.IconSize.XL" Class="text-gray-400 mb-4" />
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Access Denied</h2>
        <p class="text-gray-600 mb-4">You must be a League Admin to manage season settings.</p>
        <MaterialButton Style="MaterialButtonStyle.Outlined"
                      Text="Back to Season"
                      OnClick="@BackToSeason" />
    </div>
}
else
{
    <!-- Breadcrumb -->
    <div class="mb-6">
        <div class="flex items-center gap-2 text-sm text-gray-600">
            <a href="/dashboard" class="hover:text-primary-600">Dashboard</a>
            <MaterialIcon Icon="MaterialIcons.ChevronRight" Class="text-xs" />
            <a href="@($"/league/{LeagueKey}")" class="hover:text-primary-600">@leagueName</a>
            <MaterialIcon Icon="MaterialIcons.ChevronRight" Class="text-xs" />
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}")" class="hover:text-primary-600">@seasonName</a>
            <MaterialIcon Icon="MaterialIcons.ChevronRight" Class="text-xs" />
            <span class="text-gray-900">Settings</span>
        </div>
    </div>

    <!-- Page Header -->
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900">Season Settings</h1>
        <p class="text-gray-600 mt-1">Configure @seasonName settings and preferences</p>
    </div>

    <!-- Tab Navigation -->
    <div class="border-b border-gray-200 mb-8">
        <nav class="-mb-px flex space-x-8">
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}")" 
               class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Overview
            </a>
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}/events")" 
               class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Events
            </a>
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}/standings")" 
               class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Standings
            </a>
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}/players")" 
               class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Players
            </a>
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}/teams")" 
               class="border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Teams
            </a>
            <a href="@($"/league/{LeagueKey}/season/{SeasonKey}/settings")" 
               class="border-primary-600 text-primary-600 whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm">
                Settings
            </a>
        </nav>
    </div>

    <!-- Settings Form -->
    <div class="max-w-3xl">
        <!-- Basic Information -->
        <div class="bg-white rounded-lg border border-gray-200 p-6 mb-6">
            <h2 class="text-lg font-semibold text-gray-900 mb-4">Basic Information</h2>
            
            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Season Name *</label>
                    <input type="text" 
                           @bind="seasonName" 
                           required
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Season Key</label>
                    <input type="text" 
                           value="@SeasonKey" 
                           disabled
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg bg-gray-50 text-gray-500" />
                    <p class="text-xs text-gray-500 mt-1">The season key cannot be changed after creation</p>
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Start Date *</label>
                        <input type="date" 
                               @bind="startDate" 
                               required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">End Date *</label>
                        <input type="date" 
                               @bind="endDate" 
                               required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                    </div>
                </div>
            </div>

            <div class="mt-6 flex justify-end gap-3">
                <MaterialButton Style="MaterialButtonStyle.Outlined"
                              Text="Cancel"
                              OnClick="@BackToSeason" />
                <MaterialButton Style="MaterialButtonStyle.Filled"
                              Text="Save Changes"
                              OnClick="@SaveSettings" />
            </div>
        </div>

        <!-- Scoring Settings -->
        <div class="bg-white rounded-lg border border-gray-200 p-6 mb-6">
            <h2 class="text-lg font-semibold text-gray-900 mb-4">Scoring Settings</h2>

            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Scoring Format</label>
                    <select @bind="scoringFormat" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500">
                        <option value="stroke">Stroke Play</option>
                        <option value="stableford">Stableford</option>
                        <option value="match">Match Play</option>
                    </select>
                </div>

                <div class="flex items-center">
                    <input type="checkbox"
                           @bind="useHandicaps"
                           id="useHandicaps"
                           class="h-4 w-4 text-primary-600 focus:ring-primary-500 border-gray-300 rounded" />
                    <label for="useHandicaps" class="ml-2 block text-sm text-gray-700">
                        Use handicaps for scoring
                    </label>
                </div>

                <div class="flex items-center">
                    <input type="checkbox"
                           @bind="dropWorstScores"
                           id="dropWorstScores"
                           class="h-4 w-4 text-primary-600 focus:ring-primary-500 border-gray-300 rounded" />
                    <label for="dropWorstScores" class="ml-2 block text-sm text-gray-700">
                        Drop worst scores from season total
                    </label>
                </div>

                @if (dropWorstScores)
                {
                    <div class="ml-6">
                        <label class="block text-sm font-medium text-gray-700 mb-2">Number of scores to drop</label>
                        <input type="number"
                               @bind="scoresToDrop"
                               min="1"
                               max="5"
                               class="w-32 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                    </div>
                }
            </div>

            <div class="mt-6 flex justify-end gap-3">
                <MaterialButton Style="MaterialButtonStyle.Outlined"
                              Text="Cancel"
                              OnClick="@BackToSeason" />
                <MaterialButton Style="MaterialButtonStyle.Filled"
                              Text="Save Changes"
                              OnClick="@SaveSettings" />
            </div>
        </div>

        <!-- Registration Settings -->
        <div class="bg-white rounded-lg border border-gray-200 p-6 mb-6">
            <h2 class="text-lg font-semibold text-gray-900 mb-4">Registration Settings</h2>

            <div class="space-y-4">
                <div class="flex items-center">
                    <input type="checkbox"
                           @bind="allowRegistration"
                           id="allowRegistration"
                           class="h-4 w-4 text-primary-600 focus:ring-primary-500 border-gray-300 rounded" />
                    <label for="allowRegistration" class="ml-2 block text-sm text-gray-700">
                        Allow new player registration
                    </label>
                </div>

                @if (allowRegistration)
                {
                    <div class="ml-6">
                        <label class="block text-sm font-medium text-gray-700 mb-2">Registration Deadline</label>
                        <input type="date"
                               @bind="registrationDeadline"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                    </div>
                }

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Maximum Players</label>
                    <input type="number"
                           @bind="maxPlayers"
                           min="0"
                           placeholder="No limit"
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent" />
                    <p class="text-xs text-gray-500 mt-1">Leave empty for no limit</p>
                </div>
            </div>

            <div class="mt-6 flex justify-end gap-3">
                <MaterialButton Style="MaterialButtonStyle.Outlined"
                              Text="Cancel"
                              OnClick="@BackToSeason" />
                <MaterialButton Style="MaterialButtonStyle.Filled"
                              Text="Save Changes"
                              OnClick="@SaveSettings" />
            </div>
        </div>

        <div class="bg-white rounded-lg border border-gray-200 p-6 mb-6">
            <h2 class="text-lg font-semibold text-gray-900 mb-2">Season Setup</h2>
            <p class="text-sm text-gray-600 mb-6">Paste a calendar and team list to bulk create season events, teams, and season player assignments.</p>

            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Calendar</label>
                    <textarea @bind="calendarText"
                              rows="12"
                              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent font-mono text-sm"
                              placeholder="Paste week, date, and note rows here"></textarea>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Teams</label>
                    <textarea @bind="teamsText"
                              rows="12"
                              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent font-mono text-sm"
                              placeholder="Paste tab-separated team rosters here"></textarea>
                </div>

                <div class="flex items-center">
                    <input type="checkbox"
                           @bind="replaceExistingSetup"
                           id="replaceExistingSetup"
                           class="h-4 w-4 text-primary-600 focus:ring-primary-500 border-gray-300 rounded" />
                    <label for="replaceExistingSetup" class="ml-2 block text-sm text-gray-700">
                        Replace existing events, teams, and season player assignments for this season
                    </label>
                </div>

                @if (!string.IsNullOrEmpty(seasonSetupMessage))
                {
                    <div class="px-4 py-3 rounded text-sm @(seasonSetupSuccess ? "bg-green-50 border border-green-200 text-green-800" : "bg-red-50 border border-red-200 text-red-800")">
                        @seasonSetupMessage
                    </div>
                }

                @if (seasonSetupResult != null)
                {
                    <div class="rounded-lg border border-gray-200 bg-gray-50 p-4 text-sm text-gray-800 space-y-2">
                        <div><span class="font-semibold">Calendar rows parsed:</span> @seasonSetupResult.CalendarWeeksParsed</div>
                        <div><span class="font-semibold">Weeks skipped:</span> @seasonSetupResult.SkippedWeeks</div>
                        <div><span class="font-semibold">Events created:</span> @seasonSetupResult.EventsCreated</div>
                        <div><span class="font-semibold">Teams created:</span> @seasonSetupResult.TeamsCreated</div>
                        <div><span class="font-semibold">Players assigned:</span> @seasonSetupResult.PlayersAssigned</div>

                        @if (seasonSetupResult.Warnings.Count > 0)
                        {
                            <div>
                                <div class="font-semibold">Warnings</div>
                                <ul class="list-disc list-inside space-y-1 text-amber-800">
                                    @foreach (var warning in seasonSetupResult.Warnings)
                                    {
                                        <li>@warning</li>
                                    }
                                </ul>
                            </div>
                        }

                        @if (seasonSetupResult.MissingPlayers.Count > 0)
                        {
                            <div>
                                <div class="font-semibold text-red-800">Players not matched to league records</div>
                                <ul class="list-disc list-inside space-y-1 text-red-700">
                                    @foreach (var player in seasonSetupResult.MissingPlayers)
                                    {
                                        <li>@player</li>
                                    }
                                </ul>
                            </div>
                        }
                    </div>
                }
            </div>

            <div class="mt-6 flex justify-end gap-3">
                <MaterialButton Style="MaterialButtonStyle.Outlined"
                              Text="Back to Season"
                              OnClick="@BackToSeason" />
                <MaterialButton Style="MaterialButtonStyle.Filled"
                              Text="@GetApplySetupButtonText()"
                              Disabled="@(isApplyingSeasonSetup || string.IsNullOrWhiteSpace(calendarText) || string.IsNullOrWhiteSpace(teamsText))"
                              OnClick="@ApplySeasonSetup" />
            </div>
        </div>

        <!-- Danger Zone -->
        <div class="bg-white rounded-lg border border-red-200 p-6">
            <h2 class="text-lg font-semibold text-red-900 mb-4">Danger Zone</h2>

            <div class="space-y-4">
                <div class="flex items-center justify-between p-4 bg-red-50 rounded-lg">
                    <div>
                        <h3 class="text-sm font-semibold text-red-900">Archive Season</h3>
                        <p class="text-sm text-red-700 mt-1">Mark this season as archived. It will be read-only.</p>
                    </div>
                    <MaterialButton Style="MaterialButtonStyle.Outlined"
                                  Text="Archive"
                                  Disabled="@(isArchiving || season?.IsLocked == true)"
                                  OnClick="@ArchiveSeason" />
                </div>

                <div class="flex items-center justify-between p-4 bg-red-50 rounded-lg">
                    <div>
                        <h3 class="text-sm font-semibold text-red-900">Delete Season</h3>
                        <p class="text-sm text-red-700 mt-1">Permanently delete this season and all its data. This cannot be undone.</p>
                    </div>
                    <MaterialButton Style="MaterialButtonStyle.Outlined"
                                  Text="Delete"
                                  OnClick="@DeleteSeason" />
                </div>
            </div>
        </div>
    </div>
}

